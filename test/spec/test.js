/* global describe, it */

(function () {
    'use strict';

    describe('Login', function () {
        
        describe('it should prompt the user to pick a user name', function () {
            
            it('should make sure the input is valid', function () {

            	
            	
            });

            it('should assign that input to the user variable', function () {

            });
        });
    });
  
    describe('Load Chat', function () {

    	it('should grab the JSON from the server', function () {

    	});

    	it('should store that object in the data variable', function () {

    	});
    });

    describe('Render Chat', function () {

    	it('should clear the chat log',function () {

    	});

    	it('should run through each chat object, and if it has a message, render it to the page', function () {

    	});

    });

    describe('Message Constructor', function () {

    	it('should make a message instance if called', function () {

    	});

    	it('should give that instance properties of user, message, time, and meta', function () {

    	});

    });

    describe('Submit Click', function () {
    	it('should set the input from the input to a variable'. function () {

    	});

    	it('should make a Message instance called outgoing', function () {

    	});

    	it('should call render function on that instance called outgoing', function {

    	});
    });
    describe('Update Chat', function () {

    	it('should post the message instance called outgoing to the server', function () {

    	});
    });
})();
